description: Button state machine driver for button-smf library

compatible: "buttonsmf"

include: base.yaml

properties:
  input-gpios:
    type: phandle-array
    required: true
    description: Input GPIO to be sensed.
